/**
 * Twitter card image — mirrors the OG image dimensions and styling.
 * Next.js convention: a file at this path becomes the Twitter
 * summary_large_image for routes that don't define their own.
 */
export { default, runtime, alt, size, contentType } from "./opengraph-image";
